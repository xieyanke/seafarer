info:
  contact: {}
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  title: Seafarer API
  version: v0.1.0
paths:
  /ping:
    get:
      consumes:
      - text/plain
      description: do ping
      produces:
      - text/plain
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping...
      tags:
      - Demo
schemes:
- http
securityDefinitions:
  BasicAuth:
    type: basic
swagger: "2.0"
